import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dup-ion-module',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
